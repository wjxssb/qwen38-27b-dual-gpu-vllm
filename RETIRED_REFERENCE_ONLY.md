# RETIRED / REFERENCE ONLY

此目录是历史事故、排查、源码、配置和验收档案，不是生产启动或回滚入口。生产位于 `~/nvidia-dense-runtime`（本机本地路径）。

原位大型文件及小文件归档已逐文件 SHA256 独立读回验证。清单：`~/nvidia-runtime-history/VERIFIED_HISTORICAL_MANIFEST.jsonl`（本机本地档案）。不可被 systemd、Gateway、OpenCode、JIT 或自动清理任务调用/覆写。旧运行资产删除仍须最终验收门禁。
